import {ValueInterpreter} from "../../src/lib/Service/ValueInterpreter";

describe('Class: ValueInterpreter', () => {

    describe('After Instantiation', () => {
        let subject: ValueInterpreter;

        beforeEach(() => {
            subject = new ValueInterpreter('id');
        });

        describe('Method: ', () => {

        });
    });

});
